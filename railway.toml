[build]
builder = "nixpacks"

[deploy]
startCommand = "npm install && node server/index.js"
healthcheckPath = "/api/ping"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
